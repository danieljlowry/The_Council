{
  "name": "the-council-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Database (Prisma) and future API — no UI",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "db:generate": "npx prisma generate",
    "db:ping": "sh -c 'echo \"SELECT 1 AS ok;\" | npx prisma db execute --stdin --schema prisma/schema.prisma'",
    "db:migrate": "npx prisma migrate dev",
    "db:studio": "npx prisma studio",
    "db:ping:ipv4": "NODE_OPTIONS=--dns-result-order=ipv4first sh -c 'echo \"SELECT 1 AS ok;\" | npx prisma db execute --stdin --schema prisma/schema.prisma'"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@prisma/client": "^6.19.0",
    "dotenv": "^17.3.1",
    "fastify": "^4.26.0",
    "jose": "^6.2.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "prisma": "^6.19.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.7.0",
    "typescript": "^5"
  }
}
